## ======================================================================
## GENERATED FILE. DO NOT EDIT
## Generated by running ./ci/configure build-eirini-images from ~/workspace/cf-for-k8s
## ======================================================================

resources:
- name: eirini
  type: git
  source:
    uri: git@github.com:Birdrock/eirini.git
    branch: 1.9.0-backport-stop-hardcoding-uid-2000
    private_key: ((cf_for_k8s_readonly_deploy_key.private_key))
- name: eirini-staging
  type: git
  source:
    uri: git@github.com:cloudfoundry-incubator/eirini-staging.git
    branch: 1.9.0
    private_key: ((cf_for_k8s_readonly_deploy_key.private_key))
- name: eirini-release
  type: github-release
  icon: github
  source:
    owner: cloudfoundry-incubator
    repository: eirini-release
    access_token: ((cf_deployment_release_bot_access_token))
- name: cf-for-k8s-eirini-eirini-controller
  type: registry-image
  source:
    repository: relintdockerhubpushbot/cf-for-k8s-eirini-eirini-controller
    username: ((dockerhub.username))
    password: ((dockerhub.password))
    tag: 1.9.0
- name: cf-for-k8s-eirini-event-reporter
  type: registry-image
  source:
    repository: relintdockerhubpushbot/cf-for-k8s-eirini-event-reporter
    username: ((dockerhub.username))
    password: ((dockerhub.password))
    tag: 1.9.0
- name: cf-for-k8s-eirini-instance-index-env-injector
  type: registry-image
  source:
    repository: relintdockerhubpushbot/cf-for-k8s-eirini-instance-index-env-injector
    username: ((dockerhub.username))
    password: ((dockerhub.password))
    tag: 1.9.0
- name: cf-for-k8s-eirini-opi
  type: registry-image
  source:
    repository: relintdockerhubpushbot/cf-for-k8s-eirini-opi
    username: ((dockerhub.username))
    password: ((dockerhub.password))
    tag: 1.9.0
- name: cf-for-k8s-eirini-task-reporter
  type: registry-image
  source:
    repository: relintdockerhubpushbot/cf-for-k8s-eirini-task-reporter
    username: ((dockerhub.username))
    password: ((dockerhub.password))
    tag: 1.9.0
- name: cf-for-k8s-eirini-downloader
  type: registry-image
  source:
    repository: relintdockerhubpushbot/cf-for-k8s-eirini-downloader
    username: ((dockerhub.username))
    password: ((dockerhub.password))
    tag: 1.9.0
- name: cf-for-k8s-eirini-executor
  type: registry-image
  source:
    repository: relintdockerhubpushbot/cf-for-k8s-eirini-executor
    username: ((dockerhub.username))
    password: ((dockerhub.password))
    tag: 1.9.0
- name: cf-for-k8s-eirini-uploader
  type: registry-image
  source:
    repository: relintdockerhubpushbot/cf-for-k8s-eirini-uploader
    username: ((dockerhub.username))
    password: ((dockerhub.password))
    tag: 1.9.0
jobs:
- name: build-eirini-controller
  plan:
  - task: pipeline-revision-info
    config:
      platform: linux
      image_resource:
        source:
          repository: busybox
        type: docker-image
      run:
        path: /bin/echo
        args:
        - -e
        - git@github.com:cloudfoundry/cf-for-k8s.git 108e846d39720168d55a4a160e1ad8e675b9f6ca ci/templates/build-eirini-images.yml \e[31mdirty\e[0m
  - get: eirini
  - get: eirini-release
    trigger: true
  - task: rev-parse
    config:
      platform: linux
      image_resource:
        type: registry-image
        source:
          repository: bitnami/git
      inputs:
      - name: eirini
      outputs:
      - name: revision
      params:
        REPO: eirini
      run:
        path: /bin/bash
        args:
        - -c
        - |
          pushd ${REPO}
            git rev-parse HEAD > ../revision/revision
            git describe --tags > ../revision/tag
          popd > /dev/null
  - task: build-eirini-controller
    privileged: true
    config:
      platform: linux
      image_resource:
        type: registry-image
        source:
          repository: vito/oci-build-task
      inputs:
      - name: eirini
      - name: revision
      outputs:
      - name: image
      caches:
      - path: cache
      run:
        path: /bin/sh
        args:
        - -c
        - |
          export BUILD_ARG_GIT_SHA="$(cat revision/revision)"
          ls -R
          build
      params:
        DOCKERFILE: ./eirini/docker/eirini-controller/Dockerfile
        CONTEXT: ./eirini/
  - task: deplab-eirini-controller
    privileged: true
    config:
      platform: linux
      image_resource:
        type: registry-image
        source:
          repository: relintdockerhubpushbot/cf-for-k8s-deplab
      inputs:
      - name: eirini
      - name: image
      outputs:
      - name: deplab-image
      params:
        REPO: eirini
      run:
        path: /bin/sh
        args:
        - -c
        - |
          deplab --image-tar image/image.tar \
            --git ${REPO} \
            --output-tar deplab-image/image.tar
  - put: cf-for-k8s-eirini-eirini-controller
    params:
      image: deplab-image/image.tar
- name: build-event-reporter
  plan:
  - task: pipeline-revision-info
    config:
      platform: linux
      image_resource:
        source:
          repository: busybox
        type: docker-image
      run:
        path: /bin/echo
        args:
        - -e
        - git@github.com:cloudfoundry/cf-for-k8s.git 108e846d39720168d55a4a160e1ad8e675b9f6ca ci/templates/build-eirini-images.yml \e[31mdirty\e[0m
  - get: eirini
  - get: eirini-release
    trigger: true
  - task: rev-parse
    config:
      platform: linux
      image_resource:
        type: registry-image
        source:
          repository: bitnami/git
      inputs:
      - name: eirini
      outputs:
      - name: revision
      params:
        REPO: eirini
      run:
        path: /bin/bash
        args:
        - -c
        - |
          pushd ${REPO}
            git rev-parse HEAD > ../revision/revision
            git describe --tags > ../revision/tag
          popd > /dev/null
  - task: build-event-reporter
    privileged: true
    config:
      platform: linux
      image_resource:
        type: registry-image
        source:
          repository: vito/oci-build-task
      inputs:
      - name: eirini
      - name: revision
      outputs:
      - name: image
      caches:
      - path: cache
      run:
        path: /bin/sh
        args:
        - -c
        - |
          export BUILD_ARG_GIT_SHA="$(cat revision/revision)"
          ls -R
          build
      params:
        DOCKERFILE: ./eirini/docker/event-reporter/Dockerfile
        CONTEXT: ./eirini/
  - task: deplab-event-reporter
    privileged: true
    config:
      platform: linux
      image_resource:
        type: registry-image
        source:
          repository: relintdockerhubpushbot/cf-for-k8s-deplab
      inputs:
      - name: eirini
      - name: image
      outputs:
      - name: deplab-image
      params:
        REPO: eirini
      run:
        path: /bin/sh
        args:
        - -c
        - |
          deplab --image-tar image/image.tar \
            --git ${REPO} \
            --output-tar deplab-image/image.tar
  - put: cf-for-k8s-eirini-event-reporter
    params:
      image: deplab-image/image.tar
- name: build-instance-index-env-injector
  plan:
  - task: pipeline-revision-info
    config:
      platform: linux
      image_resource:
        source:
          repository: busybox
        type: docker-image
      run:
        path: /bin/echo
        args:
        - -e
        - git@github.com:cloudfoundry/cf-for-k8s.git 108e846d39720168d55a4a160e1ad8e675b9f6ca ci/templates/build-eirini-images.yml \e[31mdirty\e[0m
  - get: eirini
  - get: eirini-release
    trigger: true
  - task: rev-parse
    config:
      platform: linux
      image_resource:
        type: registry-image
        source:
          repository: bitnami/git
      inputs:
      - name: eirini
      outputs:
      - name: revision
      params:
        REPO: eirini
      run:
        path: /bin/bash
        args:
        - -c
        - |
          pushd ${REPO}
            git rev-parse HEAD > ../revision/revision
            git describe --tags > ../revision/tag
          popd > /dev/null
  - task: build-instance-index-env-injector
    privileged: true
    config:
      platform: linux
      image_resource:
        type: registry-image
        source:
          repository: vito/oci-build-task
      inputs:
      - name: eirini
      - name: revision
      outputs:
      - name: image
      caches:
      - path: cache
      run:
        path: /bin/sh
        args:
        - -c
        - |
          export BUILD_ARG_GIT_SHA="$(cat revision/revision)"
          ls -R
          build
      params:
        DOCKERFILE: ./eirini/docker/instance-index-env-injector/Dockerfile
        CONTEXT: ./eirini/
  - task: deplab-instance-index-env-injector
    privileged: true
    config:
      platform: linux
      image_resource:
        type: registry-image
        source:
          repository: relintdockerhubpushbot/cf-for-k8s-deplab
      inputs:
      - name: eirini
      - name: image
      outputs:
      - name: deplab-image
      params:
        REPO: eirini
      run:
        path: /bin/sh
        args:
        - -c
        - |
          deplab --image-tar image/image.tar \
            --git ${REPO} \
            --output-tar deplab-image/image.tar
  - put: cf-for-k8s-eirini-instance-index-env-injector
    params:
      image: deplab-image/image.tar
- name: build-opi
  plan:
  - task: pipeline-revision-info
    config:
      platform: linux
      image_resource:
        source:
          repository: busybox
        type: docker-image
      run:
        path: /bin/echo
        args:
        - -e
        - git@github.com:cloudfoundry/cf-for-k8s.git 108e846d39720168d55a4a160e1ad8e675b9f6ca ci/templates/build-eirini-images.yml \e[31mdirty\e[0m
  - get: eirini
  - get: eirini-release
    trigger: true
  - task: rev-parse
    config:
      platform: linux
      image_resource:
        type: registry-image
        source:
          repository: bitnami/git
      inputs:
      - name: eirini
      outputs:
      - name: revision
      params:
        REPO: eirini
      run:
        path: /bin/bash
        args:
        - -c
        - |
          pushd ${REPO}
            git rev-parse HEAD > ../revision/revision
            git describe --tags > ../revision/tag
          popd > /dev/null
  - task: build-opi
    privileged: true
    config:
      platform: linux
      image_resource:
        type: registry-image
        source:
          repository: vito/oci-build-task
      inputs:
      - name: eirini
      - name: revision
      outputs:
      - name: image
      caches:
      - path: cache
      run:
        path: /bin/sh
        args:
        - -c
        - |
          export BUILD_ARG_GIT_SHA="$(cat revision/revision)"
          ls -R
          build
      params:
        DOCKERFILE: ./eirini/docker/opi/Dockerfile
        CONTEXT: ./eirini/
  - task: deplab-opi
    privileged: true
    config:
      platform: linux
      image_resource:
        type: registry-image
        source:
          repository: relintdockerhubpushbot/cf-for-k8s-deplab
      inputs:
      - name: eirini
      - name: image
      outputs:
      - name: deplab-image
      params:
        REPO: eirini
      run:
        path: /bin/sh
        args:
        - -c
        - |
          deplab --image-tar image/image.tar \
            --git ${REPO} \
            --output-tar deplab-image/image.tar
  - put: cf-for-k8s-eirini-opi
    params:
      image: deplab-image/image.tar
- name: build-task-reporter
  plan:
  - task: pipeline-revision-info
    config:
      platform: linux
      image_resource:
        source:
          repository: busybox
        type: docker-image
      run:
        path: /bin/echo
        args:
        - -e
        - git@github.com:cloudfoundry/cf-for-k8s.git 108e846d39720168d55a4a160e1ad8e675b9f6ca ci/templates/build-eirini-images.yml \e[31mdirty\e[0m
  - get: eirini
  - get: eirini-release
    trigger: true
  - task: rev-parse
    config:
      platform: linux
      image_resource:
        type: registry-image
        source:
          repository: bitnami/git
      inputs:
      - name: eirini
      outputs:
      - name: revision
      params:
        REPO: eirini
      run:
        path: /bin/bash
        args:
        - -c
        - |
          pushd ${REPO}
            git rev-parse HEAD > ../revision/revision
            git describe --tags > ../revision/tag
          popd > /dev/null
  - task: build-task-reporter
    privileged: true
    config:
      platform: linux
      image_resource:
        type: registry-image
        source:
          repository: vito/oci-build-task
      inputs:
      - name: eirini
      - name: revision
      outputs:
      - name: image
      caches:
      - path: cache
      run:
        path: /bin/sh
        args:
        - -c
        - |
          export BUILD_ARG_GIT_SHA="$(cat revision/revision)"
          ls -R
          build
      params:
        DOCKERFILE: ./eirini/docker/task-reporter/Dockerfile
        CONTEXT: ./eirini/
  - task: deplab-task-reporter
    privileged: true
    config:
      platform: linux
      image_resource:
        type: registry-image
        source:
          repository: relintdockerhubpushbot/cf-for-k8s-deplab
      inputs:
      - name: eirini
      - name: image
      outputs:
      - name: deplab-image
      params:
        REPO: eirini
      run:
        path: /bin/sh
        args:
        - -c
        - |
          deplab --image-tar image/image.tar \
            --git ${REPO} \
            --output-tar deplab-image/image.tar
  - put: cf-for-k8s-eirini-task-reporter
    params:
      image: deplab-image/image.tar
- name: build-downloader
  plan:
  - task: pipeline-revision-info
    config:
      platform: linux
      image_resource:
        source:
          repository: busybox
        type: docker-image
      run:
        path: /bin/echo
        args:
        - -e
        - git@github.com:cloudfoundry/cf-for-k8s.git 108e846d39720168d55a4a160e1ad8e675b9f6ca ci/templates/build-eirini-images.yml \e[31mdirty\e[0m
  - get: eirini-staging
  - get: eirini-release
    trigger: true
  - task: rev-parse
    config:
      platform: linux
      image_resource:
        type: registry-image
        source:
          repository: bitnami/git
      inputs:
      - name: eirini-staging
      outputs:
      - name: revision
      params:
        REPO: eirini-staging
      run:
        path: /bin/bash
        args:
        - -c
        - |
          pushd ${REPO}
            git rev-parse HEAD > ../revision/revision
            git describe --tags > ../revision/tag
          popd > /dev/null
  - task: build-downloader
    privileged: true
    config:
      platform: linux
      image_resource:
        type: registry-image
        source:
          repository: vito/oci-build-task
      inputs:
      - name: eirini-staging
      - name: revision
      outputs:
      - name: image
      caches:
      - path: cache
      run:
        path: /bin/sh
        args:
        - -c
        - |
          export BUILD_ARG_GIT_SHA="$(cat revision/revision)"
          ls -R
          build
      params:
        DOCKERFILE: ./eirini-staging/image/downloader/Dockerfile
        CONTEXT: ./eirini-staging/
  - task: deplab-downloader
    privileged: true
    config:
      platform: linux
      image_resource:
        type: registry-image
        source:
          repository: relintdockerhubpushbot/cf-for-k8s-deplab
      inputs:
      - name: eirini-staging
      - name: image
      outputs:
      - name: deplab-image
      params:
        REPO: eirini-staging
      run:
        path: /bin/sh
        args:
        - -c
        - |
          deplab --image-tar image/image.tar \
            --git ${REPO} \
            --output-tar deplab-image/image.tar
  - put: cf-for-k8s-eirini-downloader
    params:
      image: deplab-image/image.tar
- name: build-executor
  plan:
  - task: pipeline-revision-info
    config:
      platform: linux
      image_resource:
        source:
          repository: busybox
        type: docker-image
      run:
        path: /bin/echo
        args:
        - -e
        - git@github.com:cloudfoundry/cf-for-k8s.git 108e846d39720168d55a4a160e1ad8e675b9f6ca ci/templates/build-eirini-images.yml \e[31mdirty\e[0m
  - get: eirini-staging
  - get: eirini-release
    trigger: true
  - task: rev-parse
    config:
      platform: linux
      image_resource:
        type: registry-image
        source:
          repository: bitnami/git
      inputs:
      - name: eirini-staging
      outputs:
      - name: revision
      params:
        REPO: eirini-staging
      run:
        path: /bin/bash
        args:
        - -c
        - |
          pushd ${REPO}
            git rev-parse HEAD > ../revision/revision
            git describe --tags > ../revision/tag
          popd > /dev/null
  - task: build-executor
    privileged: true
    config:
      platform: linux
      image_resource:
        type: registry-image
        source:
          repository: vito/oci-build-task
      inputs:
      - name: eirini-staging
      - name: revision
      outputs:
      - name: image
      caches:
      - path: cache
      run:
        path: /bin/sh
        args:
        - -c
        - |
          export BUILD_ARG_GIT_SHA="$(cat revision/revision)"
          ls -R
          build
      params:
        DOCKERFILE: ./eirini-staging/image/executor/Dockerfile
        CONTEXT: ./eirini-staging/
  - task: deplab-executor
    privileged: true
    config:
      platform: linux
      image_resource:
        type: registry-image
        source:
          repository: relintdockerhubpushbot/cf-for-k8s-deplab
      inputs:
      - name: eirini-staging
      - name: image
      outputs:
      - name: deplab-image
      params:
        REPO: eirini-staging
      run:
        path: /bin/sh
        args:
        - -c
        - |
          deplab --image-tar image/image.tar \
            --git ${REPO} \
            --output-tar deplab-image/image.tar
  - put: cf-for-k8s-eirini-executor
    params:
      image: deplab-image/image.tar
- name: build-uploader
  plan:
  - task: pipeline-revision-info
    config:
      platform: linux
      image_resource:
        source:
          repository: busybox
        type: docker-image
      run:
        path: /bin/echo
        args:
        - -e
        - git@github.com:cloudfoundry/cf-for-k8s.git 108e846d39720168d55a4a160e1ad8e675b9f6ca ci/templates/build-eirini-images.yml \e[31mdirty\e[0m
  - get: eirini-staging
  - get: eirini-release
    trigger: true
  - task: rev-parse
    config:
      platform: linux
      image_resource:
        type: registry-image
        source:
          repository: bitnami/git
      inputs:
      - name: eirini-staging
      outputs:
      - name: revision
      params:
        REPO: eirini-staging
      run:
        path: /bin/bash
        args:
        - -c
        - |
          pushd ${REPO}
            git rev-parse HEAD > ../revision/revision
            git describe --tags > ../revision/tag
          popd > /dev/null
  - task: build-uploader
    privileged: true
    config:
      platform: linux
      image_resource:
        type: registry-image
        source:
          repository: vito/oci-build-task
      inputs:
      - name: eirini-staging
      - name: revision
      outputs:
      - name: image
      caches:
      - path: cache
      run:
        path: /bin/sh
        args:
        - -c
        - |
          export BUILD_ARG_GIT_SHA="$(cat revision/revision)"
          ls -R
          build
      params:
        DOCKERFILE: ./eirini-staging/image/uploader/Dockerfile
        CONTEXT: ./eirini-staging/
  - task: deplab-uploader
    privileged: true
    config:
      platform: linux
      image_resource:
        type: registry-image
        source:
          repository: relintdockerhubpushbot/cf-for-k8s-deplab
      inputs:
      - name: eirini-staging
      - name: image
      outputs:
      - name: deplab-image
      params:
        REPO: eirini-staging
      run:
        path: /bin/sh
        args:
        - -c
        - |
          deplab --image-tar image/image.tar \
            --git ${REPO} \
            --output-tar deplab-image/image.tar
  - put: cf-for-k8s-eirini-uploader
    params:
      image: deplab-image/image.tar
